# Terraform env: paper

Phase 9 scaffold. Concrete provider credentials + state backends
are applied alongside the deployment; Phase 9 commits the env
layout so promotions (dev -> paper -> production) follow the same
module wiring.
