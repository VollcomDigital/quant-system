# kubernetes_cluster

Phase 9 Terraform module scaffold. Concrete provider resources land
alongside the Phase 9 deployment playbooks; Phase 9 commits the module
surface + inputs + outputs so envs can wire modules together.
