# shared_lib

Phase 0 scaffold for the `shared_lib` domain package.

- Status: scaffold only (no production code yet).
- Owns: see `docs/architecture/phase-0-scaffold.md`.
- Canonical import root: `shared_lib.<module>`.
- Package-local manifest: `./pyproject.toml` (ADR-0001).

Do not add runtime logic here until the matching phase is active:

- `shared_lib` — Phase 1
- `data_platform` — Phase 2
- `alpha_research` — Phase 3
- `backtest_engine` — Phase 4
- `ai_agents` — Phase 5
- `trading_system` — Phase 6 onward
